# 프로덕션(EC2 등)용. 동일 디렉터리에 .env 파일(GROQ_API_KEY=...) 필요.
services:
  api:
    build: .
    image: gains-and-guide-ai:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      PORT: "8000"
    restart: unless-stopped
